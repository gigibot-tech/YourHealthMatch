{
  "name": "your-health-match",
  "version": "1.0.0",
  "description": "YourHealthMatch telemedicine video — Netlify web client + Agora token API",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static public/ + Netlify Functions ready')\"",
    "start": "npm --prefix backend start",
    "dev:api": "npm --prefix backend run dev"
  },
  "dependencies": {
    "agora-access-token": "^2.0.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}
